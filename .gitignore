# Agentic project files (always gitignored, never committed)
CLAUDE.md
MEMORY.md
ANTIPATTERNS.md
FRAMEWORK.md
PLAN.md


# Plan + experiment artifacts (ledger persists locally)
ledger/
papers/

# Experiments: ignore everything inside by default, but re-include the
# official evolutionary iteration dirs so spec + run.py + manifest travel
# through git to the cluster (no more inline-JSON sbatch --wrap).
# Cluster outputs (result.json, trace.jsonl, checkpoints) stay ignored.
experiments/*
!experiments/iter_*/
# Canonical workflow: experiment artifacts travel ONLY via git push/pull.
# Cluster commits result.json + trace.jsonl back; Mac pulls; Claude reads
# locally. NO terminal forensics, NO paste-text-into-chat (ANTIPATTERN 11a).
# Only heavy weights (checkpoints/) stay out of git.
experiments/iter_*/**/checkpoints/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
.pytest_cache/
.mypy_cache/

# Virtual environments
.venv/
venv/
ENV/

# Jupyter
.ipynb_checkpoints/

# Data (challenge data is not redistributable)
data/raw/
data/features/
data/signals/
data/interim/
*.mat
*.h5
*.hdf5
*.pkl
*.pickle
*.parquet

# SQLite databases (ledger lives here)
*.db
*.db-journal
*.sqlite
*.sqlite3

# Model checkpoints and large weights
checkpoints/
*.pth
*.pt
*.ckpt

# LaTeX build artifacts
*.aux
*.log
*.out
*.fls
*.fdb_latexmk
*.synctex.gz
*.bbl
*.blg
*.toc

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude session state
.claude/

# Cluster / SLURM
slurm-*.out
*.err
logs/
