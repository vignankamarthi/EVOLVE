# AI4Pain-2026 Python dependencies
# NOTE: NO external LLM SDKs (no anthropic, openai, langchain, llamaindex).
# The mutation operator is the Claude Code session, not a programmatic client.
# NOTE: NO cluster libraries (no paramiko, no fabric, no asyncssh).
# All cluster operations are manual per HIP-D, HIP-E, HIP-F.

# Numerics + data
numpy>=1.26
pandas>=2.1
scipy>=1.12
pyarrow>=15.0

# Convex optimization (cvxEDA tonic+phasic decomposition, Greco et al. 2016).
# Used by ai4pain.eda_decomp. Cluster must run `pip install -r requirements.txt`
# after pulling iter_0012 for this dep to be available.
cvxpy>=1.4
cvxopt>=1.3

# ML
scikit-learn>=1.4
pycatch22>=0.4
# torch is INTENTIONALLY NOT listed here. Install per platform:
#   Mac (local dev):  pip install torch
#   Explorer cluster: pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
# Reason: Explorer's H200 driver (CUDA 12.0) is incompatible with torch>=2.11
# (which requires CUDA 13). Cluster MUST pin to 2.5.1+cu121. Mac runs default.

# Utilities
tqdm>=4.66

# TDD (per ANTIPATTERNS rule 21: no implementation without failing test first)
pytest>=7.4
pytest-cov>=4.1
